import { InjectionToken } from '@angular/core';

export const Weather_Report_Days = new InjectionToken("Weather Report Days");

export const Weather_Forecast_Days = new InjectionToken("Weather Forecast Days");